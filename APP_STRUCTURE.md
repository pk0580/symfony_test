# Структура приложения Symfony 7.4

Добро пожаловать в мир Symfony! Этот документ поможет тебе разобраться, как устроено твое новое приложение, что находится в каждой папке и как все эти части взаимодействуют друг с другом.

---

## 📂 Обзор директорий

### `assets/`
Здесь хранятся исходные файлы фронтенда: CSS, JavaScript и изображения. В современных версиях Symfony (с использованием **AssetMapper**) эти файлы напрямую подключаются к шаблонам без сложной сборки через Webpack.
- `assets/styles/`: Ваши стили (CSS).
- `assets/controllers/`: Контроллеры **Stimulus** для добавления интерактивности на страницы.

### `bin/`
Содержит исполняемые файлы. Самый важный из них — `bin/console`. Это мощный инструмент командной строки Symfony, который позволяет создавать контроллеры, сущности, выполнять миграции и очищать кеш.
- Пример: `php bin/console make:controller`

### `config/`
Сердце конфигурации приложения. Здесь настраиваются маршруты (routes), пакеты (bundles) и параметры самого сервиса.
- `packages/`: Настройки отдельных библиотек (Doctrine, Twig, Security).
- `routes.yaml` (или атрибуты в контроллерах): Описывает, какой URL какому контроллеру соответствует.
- `services.yaml`: Настройка сервисов и внедрения зависимостей (Dependency Injection).

### `migrations/`
Когда вы меняете структуру базы данных через Doctrine, здесь создаются PHP-файлы с SQL-запросами. Это история изменений вашей БД, позволяющая синхронизировать ее структуру между разработчиками.

### `public/`
Единственная папка, доступная из интернета. Веб-сервер (Nginx/Apache) должен смотреть именно сюда.
- `index.php`: Точка входа в приложение. С него начинается обработка любого запроса.

### `src/`
Здесь живет весь ваш PHP-код. Это то место, где вы будете проводить 90% времени.
- `Controller/`: Обрабатывают входящие запросы и возвращают ответы.
- `Entity/`: PHP-классы, представляющие таблицы в базе данных.
- `Repository/`: Классы для выполнения запросов к базе данных (поиск пользователей, статей и т.д.).
- `Kernel.php`: Основной класс ядра системы, который связывает все воедино.

### `templates/`
Здесь лежат шаблоны на языке **Twig**. Twig — это мощный и безопасный шаблонизатор, который позволяет разделять PHP-логику и HTML-верстку.
- `base.html.twig`: Базовый макет, от которого наследуются другие страницы.

### `tests/`
Место для ваших тестов (Unit, Functional). Symfony поставляется с поддержкой PHPUnit "из коробки".

### `translations/`
Файлы для локализации (перевода) вашего приложения на разные языки.

### `var/`
Временные файлы, которые генерирует Symfony.
- `cache/`: Скомпилированный код для ускорения работы.
- `log/`: Журналы ошибок и действий (логи).

### `vendor/`
Здесь находятся все сторонние библиотеки, установленные через Composer (включая сам Symfony). **Никогда не меняйте код в этой папке!**

---

## 🚀 Как это работает (Жизненный цикл запроса)

Представь, что пользователь ввел в браузере `https://your-site.com/hello`. Что происходит внутри?

1.  **Точка входа**: Запрос попадает в `public/index.php`.
2.  **Ядро (Kernel)**: Инициализируется ядро системы и загружаются все настройки из `config/`.
3.  **Маршрутизация (Routing)**: Symfony смотрит в настройки маршрутов и ищет: "Кто отвечает за URL `/hello`?".
4.  **Контроллер (Controller)**: Система находит нужный класс в `src/Controller` и вызывает его метод.
5.  **Бизнес-логика**: Контроллер может попросить данные у **Entity/Repository** или вызвать какой-то сервис.
6.  **Шаблон (Template)**: Контроллер передает данные в шаблон `templates/xxx.html.twig`.
7.  **Ответ (Response)**: Twig превращает шаблон в обычный HTML, контроллер упаковывает его в объект Response и отправляет обратно пользователю.

---

## 🛠 Важные файлы в корне проекта

- `.env`: Переменные окружения (настройки подключения к БД, секретные ключи). Не пушится в Git (вместо него `.env.dist` или `.env.test`).
- `composer.json`: Список всех библиотек, от которых зависит проект.
- `importmap.php`: Конфигурация JavaScript-библиотек для AssetMapper.
- `symfony.lock`: Технический файл Symfony Flex, фиксирующий состояние рецептов.

---

## 💡 Совет для новичка
Используй команду `php bin/console list`, чтобы увидеть все доступные инструменты. Symfony очень помогает разработчику, генерируя код за тебя через команды `make:...`!
